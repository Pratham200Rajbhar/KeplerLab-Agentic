Generate {language} code.

Question: {question}
Context: {context}
Constraints: {constraints}

## Requirements
1. Clean, working, complete code
2. Include runnable demo:
   ```python
   if __name__ == "__main__":
       # demo code
   ```
3. Output meaningful sample results
4. Skip demo only if user requests library/snippet only
5. Return code only — no markdown fences, no explanation unless requested
